{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "precog.serviceAccountName" . }}
  labels:
    {{- include "precog.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
